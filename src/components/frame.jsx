import React from 'react';

const Frame = () => {
    return (
        <div>
            <h1>holaaaa</h1>
            <button onClick={() => { alert('holfunciona') }}>
                click
            </button>
        </div>

    );
}

export default Frame;
